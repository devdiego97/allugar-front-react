import { Container } from "rsuite"
import { Layout } from "../../components/layout"

export const BrokerId=()=>{
    return <Layout>
        <Container>
            corretores
        </Container>
    </Layout>
}